name: Deteckt

on: 
  pull_request_target:
    branches: ["main"]
    paths:
      - '**/*.kt'
      - '!**/src/*Test*/**'
      - '!**/src/*test*/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: checkout code 
      uses: actions@checkout@v3 
