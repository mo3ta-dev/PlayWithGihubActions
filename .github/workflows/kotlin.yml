name: My Kotlin

on: 
  push: 
    paths:
      - '**/*.kt'
      - '!**/test/**/*.kt'


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Get changed files
      run: |
        echo "md is found but not in test"
